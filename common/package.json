{
  "name": "verification-service-common",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": ">=12",
    "yarn": ">=1.7"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "eslint ./src/**/*.ts"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.0.1",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "@restless/sanitizers": "^0.2.3"
  }
}
